# Политика OSV для depscan.sh: монтируется в контейнер как /rules/osv-scanner.toml и
# передаётся явным --config, поэтому osv-scanner.toml из проверяемого дерева не действует.
# Исключений здесь нет намеренно: новое [[IgnoredVulns]] или [[PackageOverrides]] — правка
# ЭТОГО файла через ревью, с причиной и сроком.
