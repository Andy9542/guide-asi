# Политика OSV для depscan.sh: он передаёт этот файл как --config, и osv-scanner.toml
# проверяемого дерева не действует. Исключения ([[IgnoredVulns]], [[PackageOverrides]])
# заводятся в этом файле, через ревью, с причиной и сроком.
