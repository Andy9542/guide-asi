# Единственная зависимость каталога: preflight.py разбирает конфиг promptfoo до вызова
# модели. Версия закреплена по той же причине, что promptfoo в run.sh и образы в других
# каталогах: плавающая версия парсера меняет то, что считается поддержанным режимом.
# Ставится в CI тем же шагом, что cryptography (.github/workflows/selftest.yml), без хэшей.
# На хосте с PEP 668 (Debian/Ubuntu) системный pip откажет: apt install python3-yaml
# (6.0.1 — самопроверка проходит и на ней) или venv.
pyyaml==6.0.3
